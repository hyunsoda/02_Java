package edu.kh.oop.cls.model.vo;

class TestVO {
	// modifier을 누르면 default를 생성해줌
	// 앞에 default가 생략되어 있음
	// 같은 패키지 내에서만 가능
	// public으로 바꾸고 싶으면 앞에 public쓰면 됨
	
	// 접근제한자 (default) : 같은 패키지내에서만 import가 가능함을 나타냄
	
}
